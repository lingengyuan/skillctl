# 更新记录

## v0.0.4 — 2026-09-08

- 修复退出进程遗留的来源缓存锁导致长时间等待的问题；使用内核锁，并兼容旧 PID 标记。
- 自动来源恢复按来源复用同步结果和错误，避免多个 Skill 重复等待同一次超时；增加恢复进度与合并诊断。
- 恢复会话复用来源目录索引，安装历史复用读取缓冲区；提供逐项消融与真实历史读取实验，仍逐项验证候选内容。
- 将代码按职责拆到 `internal` 包，保留原命令入口、版本注入和 JSON 接口；同步调整递归格式检查与集成测试。

本次未增加依赖或改变命令、JSON 接口。10 个变体、7 轮消融中，来源目录验证阶段约快 7 倍；真实历史累计分配减少 61.4%，耗时减少 5.6%。这些结果不代表整条命令的加速倍率；较慢的工具名预筛已撤掉。完整数据和复现方法见 [性能实验](docs/performance.md)，发行包附带报告与原始结果。

## v0.0.3

skillctl 从来源检查与更新工具扩展为本地 Skill 生命周期管理 CLI。现有安装保留原位置和管理者，新安装使用共享存储，并按 Agent 建立链接或复制副本。

### 新增

- 独立记录 Skill 身份、内容副本和 Agent 使用关系；支持项目发现、宿主插件归属与能力说明。
- 安装、移除、启停、分发、固定版本、差异比较、持久历史和主动回退。
- `skillctl.toml`、`skillctl.lock`、profile、导入导出、冻结与离线同步。
- 自动从结构化安装记录恢复来源；只有内容验证通过才登记。
- 搜索、配置管理、Shell 补全和显式 JSON v2；原有 JSON 接口保持兼容。

### 修复与保护

- 共享目录和链接保留每个 Agent 的归属，筛选不会丢失共享关系。
- 无效文档、断链和不可读安装保留在清单与诊断中。
- 预览不写入安装、配置或来源状态；Git 预览使用临时仓库。
- 写操作取得内核锁后读取状态，并验证前置状态、保存镜像、执行、验证和记录历史。
- 更新既有安装时同步受管理的 Agent 副本；本地修改会阻止覆盖，回退覆盖实际更新单元。
- 原生插件按已安装版本识别，整包操作展示组件范围；不把历史缓存当作已安装项。

### 兼容与使用边界

- 需要 Go 1.27 从源码构建；发行包为独立可执行文件。Git 来源仍需 Git，原生 Provider 操作仍需对应 CLI。
- `check` 可保存已验证证据和基线；需要纯预览时使用 `--dry-run`。`list` 和普通 `doctor` 不隐式初始化配置。
- `update --file FILE` 推进环境锁定版本；普通 `sync` 保持锁定版本，`sync --frozen` 不重新解析。
- Codex、Claude 插件按宿主能力执行；没有精确版本恢复接口的插件更新或移除不支持版本回退。
- bindings 表示目录暴露和插件启用状态；宿主会话内禁用、权限规则和同名优先级由宿主决定。
- 历史保留完整恢复证据，可能占用较多磁盘；GC 不删除历史引用的版本。

完整用法见 [命令参考](docs/commands.md)，事务边界见 [实现说明](docs/architecture.md)。
